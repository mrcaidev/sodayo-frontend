export interface Order {
  id: string;
  typeId: string;
  statusId: string;
  startDate: string;
  endDate: string;
  placedUserId: string;
  takenUserId: string;
  description: string;
  remark: string;
}
